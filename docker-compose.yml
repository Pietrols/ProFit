services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: profit
      POSTGRES_PASSWORD: profit_dev_pass
      POSTGRES_DB: profit
    ports:
      - "5433:5432"
